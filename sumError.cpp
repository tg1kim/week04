include

pragma

main
{
	x, y, z, sum
	"3개의 정수를 입력하세요 (x, y, z): "
	x, y, z				// x, y, z에 입력
	sum									// sum에 x를 더한다
	sum									// sum에 y를 더한다
	sum									// sum에 z를 더한다
	"3개 정수의 합은 %d\n"
}